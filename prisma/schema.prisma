generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}


model Article {
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  title     String @id 
  content   String?
}
